# 導入判断メニュー（2026-07-06 夜間作業成果）

> 夜間に「他事業所展開の品質チェック（6月レディネス作業）」の統合と、Fableによる包括セキュリティ監査を実施した。
> 本メニューの各項目は**「これ入れて」の一言で即着手できる状態**まで準備済み。
> **不可逆な操作（push・本番反映・DB変更・削除）は一切行っていない。** 全作業はローカルブランチ `feat/readiness-security-2026-07` にあり、gitで完全に巻き戻せる。

## セキュリティ監査の総評（詳細は下部）

マルチテナントRLS基盤・認可・秘密情報管理・AIへの個人情報流路は**堅牢**。
**商用展開の実質ブロッカーは1件のみ**：🔴R-1（レガシーテーブル `allowed_users`/`app_settings` が全事業所ユーザーから読め、スタッフ氏名・メール・旧パスワードハッシュが越境漏洩する）。

---

## A. ブランチに実装済み（「マージして」でmaster反映→本番デプロイ）

| # | 項目 | 内容 | リスク |
|---|------|------|--------|
| A-1 | 6月レディネス実装一式 | 監査ログ基盤／規約・プライバシーページ／問い合わせフォーム／写真Storage分離コード／GAS通知／Permissions-Policy | 低（tsc/lint済。**ただしA-4のDB適用とセット必須**） |
| A-2 | AI送信の監査記録（今夜実装） | 全12 AIルートに `ai_send` ログ（越境送信の説明責任・3省2ガイドライン対応）。migration 016未適用でも無害にスキップ | 極低（fire-and-forget） |
| A-3 | パスワード強化（今夜実装） | 最小長4→8文字＋定数時間比較（タイミング攻撃対策） | 極低 |
| A-4 | CSP Report-Only（今夜実装） | 違反をブロックせず計測のみ（挙動不変）。将来のenforce化の準備 | ゼロ（Report-Only） |

**マージ手順**: ブランチをpush→Vercel previewで動作確認→master merge（コードだけ先行可だが、A-1のStorage分離はB-1とセットで）。

## B. SQL準備済み（Supabaseダッシュボードで実行するだけ・全て検証/切り戻しSQL付き）

| # | 項目 | ファイル | 前提・注意 |
|---|------|---------|-----------|
| B-1 | 写真Storageのテナント分離 | `migrations/015` ＋ `manual/015_verify.sql`（適用前に0件確認）/ `015_rollback.sql` | A-1マージとセット。適用後に実機で写真登録/表示を確認 |
| B-2 | 監査ログテーブル | `migrations/016` | 適用後、保存操作→audit_logsに記録確認 |
| B-3 | 問い合わせテーブル | `migrations/017` | 適用後、/contactから試送信 |
| B-4 | 参加コード16桁化（今夜作成） | `migrations/018_join_code_hardening.sql` | 監査指摘Y-1①。既存コードは有効なまま（無影響）。既存分も強化するなら適用後に管理画面で再発行 |
| B-5 | 🔴R-1暫定封じ（今夜作成） | `manual/R1_interim_lockdown.sql` / `R1_interim_rollback.sql` | レガシーテーブルの越境SELECTを即遮断（対症療法）。**恒久対応はC-1**。011適用済み環境でのみ実行 |

## C. 承認後に実装着手（設計済み・所要目安つき）

| # | 項目 | 内容 | 所要 | 判断ポイント |
|---|------|------|------|-------------|
| C-1 | 🔴R-1恒久対応：レガシー完全撤去 | `allowed_users`/`app_settings.org_password` のdrop、`admin/users`・`admin/password`・`check-access`・`setup` API削除、AuthGateフォールバック除去 | 1セッション | **データ削除を伴う不可逆操作**。全端末が011適用済みか事前確認。drop前にテーブルをCSVエクスポートして保全する手順込みで実施 |
| C-2 | 招待制のみへ移行（参加コード廃止） | 総当たりリスクの根絶（監査Y-1③）。オンボーディングをメール招待（013）のみに | 半セッション | UX判断：口頭で参加コードを伝える現運用を捨てるか。B-4（16桁化）で当面は十分 |
| C-3 | 監査ログのサーバ側化 | クライアント発行→APIルート/Server Action発行へ（改ざん耐性向上・監査Y-2） | 1セッション | 3省2ガイドライン「真正性」の水準を上げたい時。当面は「限定的証跡」と運用手順書に明記で可 |
| C-4 | CSP enforce化 | A-4のReport-Onlyで違反ゼロを確認後、本番CSPへ | 短 | Report-Only計測期間（1〜2週間）を置いてから |

## D. 人間側の作業（コード外・6月TODOから継続）

| # | 項目 | 参照 |
|---|------|------|
| D-1 | 法務文書の確定（規約・プライバシーの〔 〕記入＋専門家レビュー） | `docs/本番反映TODO_2026-06-06.md` B-3 |
| D-2 | Anthropic越境送信の整理（ZDR要否・利用者同意）※**展開ブロッカー** | 同 B-4。A-2のai_sendログが説明材料になる |
| D-3 | GAS問い合わせ通知のデプロイ（無料・任意） | 同 B-5 |
| D-4 | Supabase Pro（バックアップ/BCP・月$25）／Sentry／Stripe | 同 D。有料判断 |

---

## 推奨の進め方（アテナ案）

1. **朝イチ**: B-5（R-1暫定封じ・5分）→ 越境漏洩を即遮断
2. **今週中**: A一式をpreview確認→masterマージ＋B-1〜B-4を順に適用
3. **展開準備期**: C-1（レガシー撤去・エクスポート保全つき）→ D-2（ZDR/同意）→ D-1（法務）
4. C-2/C-3/C-4は展開後の段階対応で可

---

## 付録: セキュリティ監査結果（2026-07-06・Fable＋セキュリティ監査エージェント）

### 🔴 要修正（展開前必須）
- **R-1**: `allowed_users`/`app_settings` のRLSが「認証済みなら誰でもSELECT可」（002:57-60,75-78）。新規事業所ユーザーが既定事業所のスタッフ氏名・メール・role・org_passwordのscryptハッシュを読める。→ 暫定封じ=B-5／恒久=C-1

### 🟡 推奨
- **Y-1**: 参加コード8桁＋RPCレート制限なし→総当たりで他事業所参加の理論リスク → B-4(16桁)＋C-2(招待制)
- **Y-2**: 監査ログがクライアント発行（偽エントリ注入可・消去は不可） → C-3
- **Y-3**: org_password最小長4文字 → **今夜対応済み(A-3)**（C-1撤去なら不要になる）
- **Y-4**: verifyPasswordが非定数時間比較 → **今夜対応済み(A-3)**
- **Y-5**: CSP未導入 → **今夜Report-Only導入済み(A-4)** → enforce化はC-4

### ✅ 問題なし確認済み
全12 AIルート＋contact-notifyの認証／org境界はRLSが実境界でpatientIdすり替え不可（読み書き二重防御）／レガシーadmin APIの権限昇格不可／migrations 015-017のRLS定義に漏れ・過剰なし／service_roleキー未使用／秘密情報のハードコードなし・NEXT_PUBLIC適正／AIへの個人情報流路は年齢・主病名・要介護度のみ（氏名等の構造化経路なし）／プロンプトインジェクションは人間確認前提で実害経路なし／GAS通知はTOKEN検証・SSRF不可・踏み台化なし／SECURITY DEFINER関数のsearch_path固定／XSS表面極小／セキュリティヘッダ適正
